import SearchInput from "../components/SearchInput";

function Profile() {
  return (
    <section className="">
      <h1>hello</h1>
    </section>
  );
}

export default Profile;
// it must filter it first before displaying the result
